<UserControl x:Class="AdivinaQuien.Views.ConclucionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdivinaQuien.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Visibility="{Binding Perdio}" BorderThickness="2" CornerRadius="10,10,10,10" Margin="5">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF3838" Offset="0"/>
                    <GradientStop Color="DarkRed" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <DockPanel>
                <StackPanel Orientation="Vertical" DockPanel.Dock="Top">
                    <Label Content="Perdiste" HorizontalContentAlignment="Center" HorizontalAlignment="Center" FontSize="34" Margin="5" FontWeight="Bold" Foreground="White"/>
                    <Label Content="{Binding ServidorNombre}" HorizontalContentAlignment="Center" HorizontalAlignment="Center" FontSize="30" Margin="5" FontWeight="Bold" Foreground="White"/>
                    <Label Content="{Binding PersonajeEnemigo.Nombre}" HorizontalContentAlignment="Center" HorizontalAlignment="Center" FontSize="30" Margin="5" FontWeight="Bold" Foreground="White"/>
                    <Border Grid.Column="1" BorderThickness="2" Height="260" Width="260" BorderBrush="Black" Background="Black" CornerRadius="10" Margin="10">
                        <Image HorizontalAlignment="Center" Source="{Binding PersonajeEnemigo.Imagen}" Height="250" Width="250" Stretch="Fill" Margin="5"/>
                    </Border>
                    <Button Command="{Binding VolverCommand}" HorizontalAlignment="Center" Width="250" Content="Cerrar" FontSize="20" Margin="10" Foreground="White" VerticalAlignment="Bottom">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FF3838" Offset="1"/>
                                <GradientStop Color="#050E3C" Offset="0"/>
                            </LinearGradientBrush>
                        </Button.Background>
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10,10,10,10"></Setter>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>
        <Border Visibility="{Binding Gano}" BorderThickness="2" CornerRadius="10,10,10,10" Margin="5">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#38FF38" Offset="0"/>
                    <GradientStop Color="DarkGreen" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <DockPanel>
                <StackPanel Orientation="Vertical" DockPanel.Dock="Top">
                    <Label Content="Ganaste" HorizontalContentAlignment="Center" HorizontalAlignment="Center" FontSize="34" Margin="5" FontWeight="Bold" Foreground="White"/>
                    <Label Content="{Binding ServidorNombre}" HorizontalContentAlignment="Center" HorizontalAlignment="Center" FontSize="30" Margin="5" FontWeight="Bold" Foreground="White"/>
                    <Label Content="{Binding PersonajeEnemigo.Nombre}" HorizontalContentAlignment="Center" HorizontalAlignment="Center" FontSize="30" Margin="5" FontWeight="Bold" Foreground="White"/>
                    <Border Grid.Column="1" BorderThickness="2" Height="260" Width="260" BorderBrush="Black" Background="Black" CornerRadius="10" Margin="10">
                        <Image HorizontalAlignment="Center" Source="{Binding PersonajeEnemigo.Imagen}" Height="250" Width="250" Stretch="Fill" Margin="5"/>
                    </Border>
                    <Button Command="{Binding VolverCommand}" HorizontalAlignment="Center" Width="250" Content="Cerrar" FontSize="20" Margin="10" Foreground="White" VerticalAlignment="Bottom">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FF3838" Offset="1"/>
                                <GradientStop Color="#050E3C" Offset="0"/>
                            </LinearGradientBrush>
                        </Button.Background>
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10,10,10,10"></Setter>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
