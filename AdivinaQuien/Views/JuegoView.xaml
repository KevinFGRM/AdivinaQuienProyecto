<UserControl x:Class="AdivinaQuien.Views.JuegoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdivinaQuien.Views"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="800">
    <Grid RowDefinitions="auto,*" ColumnDefinitions=".2*, .1*" Background="WhiteSmoke">
        <Border Grid.ColumnSpan="2" BorderThickness="2" CornerRadius="10,10,10,10" Margin="10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF3838" Offset="0"/>
                    <GradientStop Color="#050E3C" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#050E3C" Offset="0"/>
                    <GradientStop Color="Purple" Offset="0.5"/>
                    <GradientStop Color="#FF3838" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Label Content="Adivina Quien" HorizontalContentAlignment="Center" FontFamily="Vivaldi" FontSize="50" Foreground="White" FontWeight="Bold"></Label>
        </Border>
        <ScrollViewer Grid.Row="1" Margin="10" ScrollViewer.VerticalScrollBarVisibility="Hidden">
            <Grid IsEnabled="{Binding PuedeAdivinar}">
                <ItemsControl ItemsSource="{Binding Person}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="6">
                            </UniformGrid>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="2" CornerRadius="10,10,10,10" Margin="5">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="WhiteSmoke" Offset="0"/>
                                        <GradientStop Color="LightGray" Offset="0.4"/>
                                        <GradientStop Color="#050E3C" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Border.BorderBrush>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#050E3C" Offset="0"/>
                                        <GradientStop Color="WhiteSmoke" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.BorderBrush>
                                <Border Background="{Binding BanderaColor}" CornerRadius="10">

                                    <DockPanel>
                                        <Button HorizontalAlignment="Right" Foreground="White" DockPanel.Dock="Top" Margin="0,5,5,0" Content="❎" Command="{Binding DataContext.OcultarCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding .}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="2,2,2,2"></Setter>
                                                </Style>
                                            </Button.Resources>
                                            <Button.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#FF3838" Offset="0"/>
                                                    <GradientStop Color="#050E3C" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Button.Background>
                                            <Button.BorderBrush>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#050E3C" Offset="0"/>
                                                    <GradientStop Color="Purple" Offset="0.5"/>
                                                    <GradientStop Color="#FF3838" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Button.BorderBrush>
                                        </Button>
                                        <StackPanel Orientation="Vertical" DockPanel.Dock="Top">
                                            <Border Grid.Column="1" BorderThickness="2" BorderBrush="Black" Background="Black" CornerRadius="10" Margin="10">
                                                <Image HorizontalAlignment="Center" Source="{Binding Imagen}" Height="200" Width="200" Stretch="Fill" Margin="5"/>
                                            </Border>
                                            <Label Content="{Binding Nombre}" HorizontalContentAlignment="Center" FontSize="16" Margin="5" FontWeight="Bold" Foreground="White"/>
                                        </StackPanel>
                                        <Border BorderThickness="1" CornerRadius="10,10,10,10" Margin="10">
                                            <Button Content="Adivinar" IsEnabled="{Binding Activo}" Foreground="White" FontWeight="Bold" FontSize="16" Padding="5" BorderThickness="0" Command="{Binding DataContext.AdivinarCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding Nombre}">
                                                <Button.Resources>
                                                    <Style TargetType="Border">
                                                        <Setter Property="CornerRadius" Value="10,10,10,10"></Setter>
                                                    </Style>
                                                </Button.Resources>
                                                <Button.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Color="#FF3838" Offset="0"/>
                                                        <GradientStop Color="#050E3C" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Button.Background>
                                                <Button.BorderBrush>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Color="#050E3C" Offset="0"/>
                                                        <GradientStop Color="Purple" Offset="0.5"/>
                                                        <GradientStop Color="#FF3838" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Button.BorderBrush>
                                            </Button>
                                        </Border>
                                    </DockPanel>
                                </Border>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>
        <Grid Grid.Row="1" Grid.Column="1">
            <ScrollViewer VerticalScrollBarVisibility="Hidden">
                <Border BorderThickness="2" CornerRadius="10,10,10,10" Margin="10">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#FF3838" Offset="0"/>
                            <GradientStop Color="#050E3C" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.BorderBrush>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#050E3C" Offset="0"/>
                            <GradientStop Color="Purple" Offset="0.5"/>
                            <GradientStop Color="#FF3838" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.BorderBrush>
                    <DockPanel Margin="20">
                        <StackPanel DockPanel.Dock="Top">
                            <Label Content="Tu Personaje" FontSize="25" FontWeight="Bold" Foreground="White" HorizontalContentAlignment="Center"/>

                            <Border BorderThickness="2" BorderBrush="Black" Background="Black" CornerRadius="10" Margin="5">

                                <Image HorizontalAlignment="Center" Source="{Binding PersonajeSeleccionado.Imagen}" Height="200" Width="200" Stretch="Fill" Margin="5"/>
                            </Border>
                            <Label Content="{Binding Turno}" FontSize="25" FontWeight="Bold" Foreground="White" HorizontalContentAlignment="Center"/>

                            <ListView Height="100" Margin="5, 0" ItemsSource="{Binding MensajesJuego}">
                                <ListView.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="10,10,10,10"></Setter>
                                    </Style>
                                </ListView.Resources>
                            </ListView>
                            <Label Content="Responder" FontSize="25" FontWeight="Bold" Foreground="White" HorizontalContentAlignment="Center"/>

                            <Grid ColumnDefinitions=".1*, .1*">
                                <Button Command="{Binding ResponderCommand}" IsEnabled="{Binding TurnoResponder}" CommandParameter="Si" Content="Si" FontSize="20" Margin="10" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Background="#050E3C">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="10,10,10,10"></Setter>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                                <Button Grid.Column="1" Command="{Binding ResponderCommand}" IsEnabled="{Binding TurnoResponder}" CommandParameter="No" Content="No" FontSize="20" Margin="10" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Background="#FF3838">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="10,10,10,10"></Setter>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </Grid>

                            <Label Content="Escribe tu pregunta" FontSize="25" FontWeight="Bold" Foreground="White" HorizontalContentAlignment="Center"/>
                            <TextBox Text="{Binding Pregunta}" IsEnabled="{Binding TurnoPreguntar}" FontSize="20" Padding="10, 0" Margin="5">
                                <TextBox.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="WhiteSmoke" Offset="0"/>
                                        <GradientStop Color="LightGray" Offset="0.4"/>
                                        <GradientStop Color="#050E3C" Offset="1"/>
                                    </LinearGradientBrush>
                                </TextBox.Background>
                                <TextBox.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="10,10,10,10"></Setter>
                                    </Style>
                                </TextBox.Resources>
                            </TextBox>
                        </StackPanel>
                        <Button Command="{Binding PreguntarCommand}" IsEnabled="{Binding TurnoPreguntar}" Content="Preguntar" FontSize="20" Margin="10" Foreground="White" FontWeight="Bold" VerticalAlignment="Bottom">
                            <Button.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#FF3838" Offset="1"/>
                                    <GradientStop Color="#050E3C" Offset="0"/>
                                </LinearGradientBrush>
                            </Button.Background>
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="10,10,10,10"></Setter>
                                </Style>
                            </Button.Resources>
                        </Button>

                    </DockPanel>
                </Border>
            </ScrollViewer>
        </Grid>
        <!--<DockPanel Grid.Row="1">
            <Label Background="Gray" Width="200" Content="{Binding DataContext.NombreJugador2, RelativeSource={RelativeSource AncestorType=Window }}" Foreground="Black"></Label>
            <Button Command="{Binding DataContext.AbrirSalaCommand, RelativeSource={RelativeSource AncestorType=Window}}" Content="abrirsala" FontSize="20" Foreground="White" VerticalAlignment="Center"></Button>
        </DockPanel>-->
    </Grid>
</UserControl>
