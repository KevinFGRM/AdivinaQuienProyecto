<UserControl x:Class="AdivinaQuien.Views.SeleccionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdivinaQuien.Views"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="800">
    <Grid IsEnabled="{Binding DesHabilitarTodo}" RowDefinitions="auto, auto, *" Background="WhiteSmoke">
        <Border BorderThickness="2" CornerRadius="10,10,10,10" Margin="10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF3838" Offset="0"/>
                    <GradientStop Color="#050E3C" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#050E3C" Offset="0"/>
                    <GradientStop Color="Purple" Offset="0.5"/>
                    <GradientStop Color="#FF3838" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Label Content="Adivina Quien" HorizontalContentAlignment="Center" FontFamily="Vivaldi" FontSize="50" Foreground="White" FontWeight="Bold"></Label>
        </Border>
        <Label Grid.Row="1" Content="{Binding MensajeEsperandoSeleccion}" HorizontalContentAlignment="Center"  FontSize="16" Margin="5" FontWeight="Bold" Visibility="{Binding EsperarConexion}"></Label>
        <ScrollViewer Grid.Row="2" Margin="10" ScrollViewer.VerticalScrollBarVisibility="Hidden">
            <Grid>
                <ItemsControl ItemsSource="{Binding Person}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="6"></UniformGrid>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="2" CornerRadius="10,10,10,10" Margin="5">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="WhiteSmoke" Offset="0"/>
                                        <GradientStop Color="LightGray" Offset="0.4"/>
                                        <GradientStop Color="#050E3C" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Border.BorderBrush>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#050E3C" Offset="0"/>
                                        <GradientStop Color="WhiteSmoke" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.BorderBrush>
                                <DockPanel>
                                    <StackPanel Orientation="Vertical" DockPanel.Dock="Top">
                                        <Border Grid.Column="1" BorderThickness="2" BorderBrush="Black" Background="Black" CornerRadius="10" Margin="10">
                                            <Image HorizontalAlignment="Center" Source="{Binding Imagen}" Height="250" Width="250" Stretch="Fill" Margin="5"/>
                                        </Border>
                                        <Label Content="{Binding Nombre}" HorizontalContentAlignment="Center" FontSize="16" Margin="5" FontWeight="Bold" Foreground="White"/>
                                    </StackPanel>
                                    <Border BorderThickness="1" CornerRadius="10,10,10,10" Margin="10">
                                        <Button Content="Seleccionar" Foreground="White" FontWeight="Bold" FontSize="16" Padding="5" BorderThickness="0" Command="{Binding DataContext.SeleccionarPersonajeCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding Nombre}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="10,10,10,10"></Setter>
                                                </Style>
                                            </Button.Resources>
                                            <Button.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#FF3838" Offset="0"/>
                                                    <GradientStop Color="#050E3C" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Button.Background>
                                            <Button.BorderBrush>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#050E3C" Offset="0"/>
                                                    <GradientStop Color="Purple" Offset="0.5"/>
                                                    <GradientStop Color="#FF3838" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Button.BorderBrush>
                                        </Button>
                                    </Border>
                                </DockPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>
        <!--<DockPanel Grid.Row="1">
            <Label Background="Gray" Width="200" Content="{Binding DataContext.NombreJugador2, RelativeSource={RelativeSource AncestorType=Window }}" Foreground="Black"></Label>
            <Button Command="{Binding DataContext.AbrirSalaCommand, RelativeSource={RelativeSource AncestorType=Window}}" Content="abrirsala" FontSize="20" Foreground="White" VerticalAlignment="Center"></Button>
        </DockPanel>-->
    </Grid>
</UserControl>
