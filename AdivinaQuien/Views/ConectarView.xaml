<UserControl x:Class="AdivinaQuien.Views.ConectarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdivinaQuien.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid RowDefinitions="auto, *" Background="WhiteSmoke">
        <Border BorderThickness="2" CornerRadius="10,10,10,10" Margin="10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF3838" Offset="0"/>
                    <GradientStop Color="#050E3C" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#050E3C" Offset="0"/>
                    <GradientStop Color="Purple" Offset="0.5"/>
                    <GradientStop Color="#FF3838" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Label Content="Adivina Quien" HorizontalContentAlignment="Center" FontFamily="Vivaldi" FontSize="50" Foreground="White" FontWeight="Bold"></Label>
        </Border>
        <Grid ColumnDefinitions=".4*, .3*" Grid.Row="1" Margin="10">
            <Border BorderThickness="2" CornerRadius="10,10,10,10" Margin="10">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FF3838" Offset="0"/>
                        <GradientStop Color="#050E3C" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#050E3C" Offset="0"/>
                        <GradientStop Color="Purple" Offset="0.5"/>
                        <GradientStop Color="#FF3838" Offset="1"/>
                    </LinearGradientBrush>
                </Border.BorderBrush>
                <DockPanel Margin="20">
                    <StackPanel DockPanel.Dock="Top">
                        <Label Content="Ip del Servidor" FontSize="30" FontWeight="Bold" Foreground="White" HorizontalContentAlignment="Center"/>
                        <TextBox Text="{Binding ServidorIP}" FontSize="20" Margin="10">
                            <TextBox.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="WhiteSmoke" Offset="0"/>
                                    <GradientStop Color="LightGray" Offset="0.4"/>
                                    <GradientStop Color="#050E3C" Offset="1"/>
                                </LinearGradientBrush>
                            </TextBox.Background>
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="10,10,10,10"></Setter>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                        <Label Content="Nombre" FontSize="30" FontWeight="Bold" Foreground="White" HorizontalContentAlignment="Center"/>
                        <TextBox Text="{Binding JugadorNombre}" FontSize="20" Margin="10">
                            <TextBox.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="WhiteSmoke" Offset="0"/>
                                    <GradientStop Color="LightGray" Offset="0.4"/>
                                    <GradientStop Color="#050E3C" Offset="1"/>
                                </LinearGradientBrush>
                            </TextBox.Background>
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="10,10,10,10"></Setter>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                        <Label Content="{Binding MensajeError}" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalContentAlignment="Center"/>

                    </StackPanel>
                    <Button Command="{Binding DataContext.ConectarCommand, RelativeSource={RelativeSource AncestorType=Window}}" Content="Jugar" FontSize="20" Margin="10" Foreground="White" VerticalAlignment="Bottom" Click="Button_Click">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FF3838" Offset="1"/>
                                <GradientStop Color="#050E3C" Offset="0"/>
                            </LinearGradientBrush>
                        </Button.Background>
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10,10,10,10"></Setter>
                            </Style>
                        </Button.Resources>
                    </Button>

                </DockPanel>
            </Border>
            <Border Grid.Column="1" BorderThickness="2" BorderBrush="Black" Background="Black" CornerRadius="10" Margin="10">
                <Image  x:Name="Carrusel" MinWidth="300" Stretch="Fill" Margin="5"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
